---
title: 掛網追蹤 工作筆記
date: 2026-06-12
tags: [掛網追蹤, 工作筆記]
---

# 掛網追蹤 工作筆記

## ⏯️ 上次做到哪

- **最後動作**（2026-08-07）：處理「8/7 儀表板沒更新」——查出排程正常跑完、`85fd989` 已推上 GitHub，但 GitHub Pages build 卡在 `building` 達 9 小時變殭屍、堵死部署佇列，線上停在 8/6。
- **今日完成**：
  - **修好線上部署**：先試 `gh run rerun --failed` 但也被殭屍堵住無效 → 改用 `gh api -X POST .../pages/builds` 強制重建，頂替殭屍 build，線上恢復到 **8/7 05:27** ✅
  - 清掉殘留 `.git/index.lock`（防隔天 commit 卡住）
  - 修正 `維運/mac-mini排程設定/video-upload-tracker_SKILL.md` 描述「9:30→05:15」（實際註冊時刻）
  - **Mac mini 端**：確認本機乾淨，過程另發現並修好第三種 `.git` 損壞症狀（`index` 索引檔遺失→每檔 deleted＋untracked，用 `git read-tree HEAD` 重建）
  - 踩坑筆記新增：Pages 殭屍 build 的有效修法＋`.git` 三症狀對照表
- **待留意**：明天（8/8）早上確認儀表板有自動變 8/8；若又卡＝Pages 再殭屍，直接 `gh api -X POST .../pages/builds` 即可。根治仍建議把 repo 搬出 Google Drive

---

- **最後動作**（2026-06-26）：開工健檢補跑掛網 06-26＋admin 日報 06-25；用最新異動表重新比對 06-24/06-25 日報
- **今日完成**：
  - 掛網追蹤 06-26（新增 4 筆；**WD 逾期暴增 14 筆**：WD19-036-0101＋WD20-064-0692~0705 群書治要，已發 LINE）
  - admin 日報 06-25（tw5 整機網路中斷 10 分）
  - **異動表更新後重新比對**：06-23/24/25 三天主機異常**全部對應變更單**，無待追查故障——團隊密集調整 tw2/tw5 轉碼架構（CHG-155 直播頻道異常、CHG-156 amtb/education 轉碼搬遷、CHG-157 tw5網路斷線）
- **待留意**：WD 逾期 14 筆是真的卡住（群書治要 06-23 驗檔至今未掛網），值得追根因；排程今天仍未自動觸發

---

- **2026-06-25 動作**：排程自動化方案調查完成，建立 launchd「漏跑→發 LINE 提醒」安全網
- **今日完成**：
  - 補跑掛網追蹤 06-25（新增 12-045-0765/0766 驗檔；**WD19-036-0101 逾期 3 工作天，已發 LINE**）
  - 補跑 admin 日報 06-24（tw2 amtb+education wowza 77 分鐘長異常，需留意；連兩日 amtb 相關長異常）
  - **調查排程自動化**：證實 scheduled-tasks 掛在桌面 App、不可靠；headless `claude -p` 看不到 Gmail OAuth 連接器（兩條自動化路都不通）
  - **採用方案**：launchd 每天 11:30 檢查，排程沒跑就發 LINE（手機收得到）＋ macOS 通知。終端機與 launchd 環境均實測 LINE HTTP 200
- **下一步**：明天觀察排程是否仍漏；若想真自動化，最有潛力＝掛網追蹤改純 Python + IMAP（格式固定、不需 LLM、可脫離 Claude）

---

- **2026-06-23 動作**：Mac mini SSH 設定完成，掛網追蹤排程 git push 已恢復正常，GitHub Pages 成功推送
- **已完成**：
  - Mac mini 生成 ed25519 SSH key（xinrong@hwadzan.com），加入 GitHub 帳號 xinrong-rgb
  - Remote 從 HTTPS 改為 SSH：`git@github.com:xinrong-rgb/video-upload-tracker.git`
  - GitHub 加入 known_hosts，push 測試成功
  - 確認 7 個排程全部啟用中
- **下一步**：排程每日 09:39 自動執行，不需手動介入

---

- **最後動作**（2026-06-12）：整套工具建置完成並手動測試跑通全管線（掃信 → 更新 → 重建 → 逾期檢查 → push GitHub，線上版 HTTP 200）
- **已完成**：
  - `build_dashboard.py` 儀表板產生器（工作天計算跳六日、四級配色、WD 逾期判斷）
  - `data.json` 以截圖兩筆真實資料初始化（68-012-0019 待掛網、WD32-007-0561 已完成）
  - GitHub Pages 部署：https://xinrong-rgb.github.io/video-upload-tracker/
  - LINE 推播開通（「資訊日報」官方號 @537degwj，`line_notify.py`，金鑰在 `~/.config/hwadzan-line/config.json`），測試 2 次成功
  - 每日 09:30 排程 `video-upload-tracker`（掃信＋更新＋推 GitHub＋WD 逾期雙管道警示）
  - LINE 警示訊息格式已照使用者要求定稿：「編號（標題）已於 年-月-日 驗檔完成，至今超過 N 個工作天尚未掛網，請盡快掛網。感恩。」
- **等待中**：通知信 2026-06-15（週一）起才會寄到 admin@hwadzan.com，目前掃信為空屬正常
- **下一步**：週一信進來後觀察第一次自動跑的解析結果；首次自動執行若跳 Gmail/git 授權詢問，按允許一次即可

## 🗓️ 最近更動紀錄

| 日期 | 摘要 | 狀態 |
|---|---|---|
| 2026-06-12 | 建置整套掛網追蹤工具：儀表板＋GitHub Pages＋LINE 警示＋每日排程，手動測試全管線通過 | ✅✅✅ |
| 2026-06-23 | Mac mini SSH key 設定完成，remote 改為 SSH，排程 git push 恢復正常 | ✅✅✅ |
| 2026-06-25 | 補跑掛網 06-25＋admin 日報 06-24；建 launchd 漏跑→LINE 提醒安全網；證實兩條自動化路不通 | ✅✅✅ |
| 2026-06-26 | 補跑掛網 06-26（14 筆逾期）＋admin 日報 06-25；異動表更新後重比對 06-24/25，三天異常全對應變更單 | ✅✅✅ |
| 2026-07-04 | GitHub Pages 部署失敗（run 28720146157，21:26 UTC）：build✅ 但 deploy❌`Deployment failed, try again later`＝GitHub 伺服器端暫時性故障，非內容/程式問題。資料未丟，僅線上儀表板停在 07-03 版。處理＝重跑 deploy（見踩坑筆記）。**在 MacBook 上診斷，Mac mini 跑的排程** | ⚠️→ |
| 2026-08-07 | Pages build 卡 9 小時變殭屍堵死部署，線上停在 8/6；`gh run rerun` 無效，改 `gh api POST pages/builds` 強制重建修好（→8/7 05:27）。清 index.lock、修 skill 描述 9:30→05:15。Mac mini 另修好第三種 .git 損壞（index 索引檔遺失）。踩坑筆記加：殭屍修法＋.git 三症狀對照 | ✅✅✅ |

## 🕳️ 踩坑筆記

- **⚠️ .git 放 Google Drive＋兩台共用會壞（2026-07-02 遇到並修復，重要）**：本 repo 的 `.git` 也在 Google Drive 內，MacBook 與 Mac mini 都對它做 git 操作時，Drive 只同步了部分 `.git` 檔，導致 `.git/refs/heads/main` 檔案遺失（只剩殘留的 `main.lock`）＋殘留 `index.lock`，於是 `git status` 誤報「No commits yet」，看起來像整個 repo 重置、所有檔變「未提交 new file」。**其實歷史一顆都沒掉**（objects、`logs/HEAD` reflog、`origin/main` 全都在）。
  - **秒修 SOP**：① `rm -f .git/refs/heads/main.lock .git/index.lock` → ② `git update-ref refs/heads/main <reflog或origin的tip雜湊>`（本次為 `12e5a4c`，即 `refs/remotes/origin/main`）→ ③ `git status` 恢復正常。修前先 `cp -R .git` 備份。
  - **根因＆預防**：git repo 放雲端串流資料夾又兩台同時碰＝定時炸彈。**掛網的 git 操作固定只在一台跑**（避免兩台同時 commit）；長遠可把 repo 搬出 Drive、改用 GitHub 當同步。詳見記憶 [[git-in-gdrive-two-machines-corruption]]。
- **⚠️ GitHub Pages deploy 暫時性失敗（2026-07-04 首次遇到）**：workflow「pages build and deployment」的 **deploy** job 失敗，錯誤只有 `##[error]Deployment failed, try again later.`——這是 **GitHub Pages 伺服器端**的一次性抽風，不是我們的 code / index.html 有問題（build job 與 artifact 上傳都 ✅）。判別法：build✅、report-build-status✅，只有 deploy❌ 且訊息叫你「try again later」＝GitHub 端問題。
  - **秒修 SOP（哪台都能跑，不碰 git、無兩台搶 git 風險）**：`gh run rerun <RUN_ID> --repo xinrong-rgb/video-upload-tracker`，或 GitHub 網頁該 run 按「Re-run failed jobs」。它只是叫 GitHub 重新發佈已上傳好的 artifact。
  - 查最近 run：`gh run list --repo xinrong-rgb/video-upload-tracker --limit 5`；看失敗細節：`gh run view <RUN_ID> --repo xinrong-rgb/video-upload-tracker --log-failed`。
- **LINE 金鑰**：使用者第一次貼的是 Channel secret（32 字元短字串）→ 401；正確的是 Messaging API 分頁最下方的 Channel access token（150+ 字元長字串）
- **排程 UI**：桌面 App「Cowork」分頁的 Scheduled tasks 是另一套系統（顯示空的）；mcp scheduled-tasks 建的任務在「Code」分頁的側欄
- **金鑰安全**：本資料夾是公開 GitHub repo，金鑰一律放 `~/.config/hwadzan-line/`（已加 .gitignore 防呆）
- **Mac mini SSH**：Mac mini（manjishan，user: liang）原本沒有 `~/.ssh/`；MacBook（liangciling）也沒有。需在各機器分別生成 key 並加到 GitHub。Mac mini 的 key 已於 2026-06-23 設定完成。
- **排程未自動觸發（2026-06-24，待追蹤）**：當天 09:39 掛網追蹤、10:06 admin 日報、11:02 補跑檢查三個排程**全部沒有自動觸發**，最後手動補跑。當下條件：Claude Code App 有開、在前景；電源設定已確認正常（`sleep 0`、`disksleep 0`、`standby 0`，系統不睡眠），**漏跑與電源無關**。原因落在 scheduled-tasks 系統本身，未明。**明天（06-25）觀察 09:39/10:06 是否自動跑**，若仍漏則確認此台自動觸發功能壞掉。
- **本機 launchd 漏跑提醒器（2026-06-24 建置，06-25 改 LINE）**：因 scheduled-tasks 不可靠，加裝系統級 launchd 每天 **11:30** 自動檢查當天排程是否跑過，**沒跑就發 LINE 提醒「⏰ 排程提醒：請開工」**（手機收得到，不必看這台）＋ macOS 通知備援；跑了則安靜。
  - 檢查腳本：`~/.config/schedule-check/check_schedules.sh`
  - 本機 LINE 腳本：`~/.config/schedule-check/line_notify.py`（複製自掛網追蹤版，金鑰共用 `~/.config/hwadzan-line/config.json`）
  - LaunchAgent：`~/Library/LaunchAgents/com.hwadzan.schedule-check.plist`（改 plist 後需 `launchctl unload` 再 `load`）
  - 訊號＝本機標記檔 `~/.config/schedule-check/last_run.txt`（最後跑掛網追蹤的日期）。掛網追蹤 SKILL.md 步驟7 每次跑完會 `date +%Y-%m-%d >` 寫入此檔。
  - **踩坑1**：launchd 背景程序**無權限讀 GoogleDrive 雲端串流檔案**（`Operation not permitted`），所以檢查器與 LINE 腳本都放本機、讀本機標記檔。
  - **踩坑2（重要）**：headless `claude -p` **看不到 Gmail 等 OAuth 連接器**（連接器綁桌面 App／claude.ai 帳號層）。故「launchd + headless Claude 直接跑掛網/admin」走不通。已驗證（2026-06-25）。
  - 收到 LINE 後：開 Claude 說「開工」用 startup skill 補跑即可。
- **⚠️ GitHub Pages deploy 第二次卡死（2026-08-07 復發）**：8/7 05:15 排程正常跑完、commit `85fd989`（含當天資料）已成功推上 GitHub，但 run `31128322120` 的 **deploy job 卡住約 17 分鐘後被判失敗**（整個 run 跑了 20m53s，正常只要 40–54s）。症狀＝**本機 data.json/log 都更新到 8/7，但線上儀表板停在 8/6**（deploy 沒上線）。與 2026-07-04 同屬 GitHub Pages 伺服器端一次性抽風。
  - **已處置**：① `gh run rerun 31128322120 --repo xinrong-rgb/video-upload-tracker --failed` 重跑（哪台都能跑、不碰 git）→ 線上恢復到 8/7。② 清掉殘留 `.git/index.lock`（689 bytes 中斷殘檔，會經 Drive 同步到 Mac mini 害隔天 commit 卡住）。③ 把 `維運/mac-mini排程設定/video-upload-tracker_SKILL.md` 的 description 由「每天 9:30」改正為「每天 05:15」（實際註冊時刻，見排程時刻表）。
  - **判別法**：早上發現儀表板沒更新→先 `gh run list --repo xinrong-rgb/video-upload-tracker --limit 5`，若最近一筆 failure 且耗時遠超 1 分鐘＝deploy 卡死，直接 `gh run rerun <ID> --failed` 即可，資料不會丟（commit 早在 repo 裡）。
  - **補充（2026-08-07 當天追加）：`gh run rerun` 這次無效！** 因為原 build 卡在 `building` 狀態達 9 小時變殭屍，把整個部署佇列堵死，rerun 的新 run 也一起卡在 queued 進不去。**真正解掉的是直接請 Pages API 重建**：`gh api -X POST repos/xinrong-rgb/video-upload-tracker/pages/builds`（送出後 latest build 的 updated_at 會跳成當下時間、頂替殭屍 build，約 1–3 分鐘後線上更新）。判別殭屍：`gh api repos/xinrong-rgb/video-upload-tracker/pages/builds/latest` 若 status 長時間停在 `building` 且 created_at 是好幾小時前＝卡死。**下次遇到 deploy 卡死，優先用 pages/builds API POST，不要只試 gh run rerun。** 若連 API 重建也卡，最後手段＝Settings→Pages 來源切 None 存檔再切回 main 存檔，強制清殭屍。
- **⚠️ 同病根的第三種症狀：`.git/index` 索引檔遺失（2026-08-07 Mac mini 遇到並修復）**：同屬「`.git` 放 Google Drive 兩台共用」病根，但症狀與前兩種都不同。`git status` 每個檔案**同時**顯示 `deleted`＋`untracked`（詭異的兩邊都報）。真因＝`.git/index` 索引檔被 Drive 同步時整個漏傳，git 只能拿空索引去比對 HEAD 與工作目錄，於是兩邊都報。**歷史沒掉**（`git fsck --full` 確認 objects／refs 全在，只是索引快取遺失）。
  - **秒修 SOP**：① `cp -R .git` 備份到 repo 外（別放 repo 內以免被明天 commit 誤加）→ ② `git read-tree HEAD` 重建索引（安全，不動 commit、不動工作目錄檔案）→ ③ `git status` 恢復正常。
  - **三症狀對照（同一病根 [[git-in-gdrive-two-machines-corruption]]）**：① refs 遺失→假性「No commits yet」（修：`git update-ref`）；② index.lock 殘留→commit 卡住（修：`rm -f *.lock`）；③ index 索引檔遺失→每檔 deleted＋untracked（修：`git read-tree HEAD`）。判別靠 `git status` 的具體症狀，三者修法不同但根因相同。
